{
  "name": "formlight",
  "version": "1.0.0",
  "description": "A lightweight Form Builder web app using Vue.js.",
  "private": true,
  "scripts": {
    "start": "concurrently 'npm run start:watch --prefix server' 'npm run serve --prefix client'",
    "test": "npm test --prefix server && npm test --prefix client",
    "server:start": "npm run start --prefix server",
    "server:start:watch": "npm run start:watch --prefix server",
    "client:serve": "npm run serve --prefix client",
    "client:build": "npm run build --prefix client",
    "client:lint": "npm run lint --prefix client",
    "client:test": "npm run test --prefix client"
  },
  "author": "Andy Bettisworth",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}
